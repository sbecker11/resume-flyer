/**
 * Default `python -m` module for the **resume-parser** package.
 * Override with env `RESUME_PARSER_MODULE` if you use a different entry point.
 */
export const DEFAULT_RESUME_PARSER_PYTHON_MODULE = 'resume_parser.resume_to_flyer'
