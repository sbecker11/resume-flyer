node_modules/
.env
coverage/

# Vite production build output
dist/
color-palette-utils-ts/coverage/
.coverage

# macOS folder metadata (any directory)
.DS_Store

# Persisted UI state (per-user; app writes when a persistent attribute is updated)
app_state.json

# Local-only Cursor command (do not push)
.cursor/commands/env.md

# Generated parsed resumes (runtime output)
parsed_resumes

# Schema validator test venv
.venv-schema/.venv-schema/
